# Exp00.3 重复、近重复与泄漏风险审计

状态：**PASS（审计完成），泄漏 Gate：STOP**。

- SHA256 exact duplicate：0 组。
- near duplicate：88 个连通组，覆盖 252/993 张图片（25.38%），共 265 条相似边；最大组 11 张。
- 判定口径：64-bit pHash Hamming ≤ 6、64-bit dHash Hamming ≤ 8、32×32 标准化灰度相关性 ≥ 0.98。
- 人工抽查 contact sheet 后确认主要为同一裂纹、叶片或部位的轻微位移/光照/视角变化，并非由孔探黑边导致的普遍误报。
- 37/88 个近重复组的“类别+实例数签名”不一致，其中 22 组类别集合不一致，15 组仅实例数不同。
- 典型冲突包括高度相似画面分别标为 Dent 与 Material missing，也存在 Tears、Tip curl 等跨类冲突。
- 多个冲突跨越 JSON 5.0.1 与 2026.8.3.0 批次，说明类别口径问题可能与数据合并/重标批次有关；在专家确认前禁止自动合并或改名。

全部图片均为纯数字 stem，且有长连续编号段；图片没有 EXIF 时间，JSON 也没有真实设备/视频/工件 ID。`img_name_list` 仅重复当前文件名，无法恢复来源。因此连续帧与同一部件跨 split 泄漏风险高，Exp01 必须 group-aware，并应尽量向数据提供方取得真实来源分组。
